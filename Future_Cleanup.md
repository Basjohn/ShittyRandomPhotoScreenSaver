# Future Cleanup

Last updated: 2026-07-15

## Priority Guidance

- Promote only when the item blocks active runtime-health work or has fresh log evidence.
- Remaining direct `QTimer.singleShot` sites are mostly UI-local polish/debounce helpers; promote only if fresh evidence ties one to lifecycle, widget startup, settings return, or runtime churn.
- Stale exported settings examples are documentation hygiene, not runtime risk; batch with a defaults/doc refresh rather than interrupting active lifecycle/perf work.
- Compatibility-shell cleanup should remain low priority unless it causes import/runtime ambiguity.

Low-priority cleanup items discovered during unrelated work. These are not active tasks unless promoted into `Current_Plan.md`.

## 2026-07-14 Repository Debris And Policy Audit

The audit covered tracked and ignored artifacts, generated `.sst` defaults, exact and semantic duplication, unreferenced compatibility code, absolute-path test coupling, thread/timer ownership, and credential exposure. The two unresolved highest-risk findings below are promoted into `Current_Plan.md`; this section preserves their evidence and safe exit bars rather than acting as a second active plan. Their execution order is deprecated Blob removal, the longer compiled Steam bars, and then audit-document classification. Resolved Gmail ownership, visualizer-baseline, and canonical-SST findings now live in `Docs/Historical_Bugs.md` and the focused harness contracts rather than remaining as duplicate cleanup tasks.

- [ ] **Promoted: remove the deprecated Blob visualizer end to end rather than repairing it.** The current footprint is 267 collected tests plus Blob references across 115 files / 5,955 matching lines, including its dev gate, defaults, settings model/UI, runtime builder/binding, shader/preset assets, packaging, docs, and compatibility tests. The default pytest skip is temporary containment only. Preserve migration safety by stripping legacy Blob keys without re-emitting them, map a saved Blob selection to the registry-owned supported default, and retain absence-based tests proving startup, settings, presets, and generated defaults no longer expose the mode. Shared audio, animation, compositor, and supported visualizer behavior are out of scope.
- [ ] **Promoted: classify the ignored `audits/` tree before it can be treated as canonical.** Nine local Markdown files, roughly 167 KB, are ignored and untracked while earlier navigation text linked several as durable project authorities. Review each for unique architecture evidence, secrets, machine-specific data, and stale conclusions; deliberately track sanitized durable documents or migrate their still-valid content into canonical docs and retire the local copy. Do not blindly unignore/upload the directory. End with an automated link bar proving every canonical `Index.md` / `Current_Plan.md` document reference exists in tracked source.
- [ ] Retire tracked runtime/image debris after clean-checkout recreation bars: [cache/reddit/_startup_attempt.touch](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/cache/reddit/_startup_attempt.touch), [cache/reddit/_startup_gate.touch](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/cache/reddit/_startup_gate.touch), `tmp/steam_achievement_preview.png`, `tmp/steam_achievement_preview_segoe.png`, and `tmp/steam_connect_preview.png`. `tmp/` is ignored but these files remain tracked; prove Reddit startup state and any documentation/test image consumers recreate safely, then remove the artifacts from Git without deleting unrelated local diagnostics.
- [ ] Consolidate the exact 18,776-byte duplicate [scripts/build_runner.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/scripts/build_runner.py) and [tools/build_runner.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/tools/build_runner.py). No repository caller currently distinguishes them. Select one documented entry point, retain a thin forwarding shim only if packaged/external invocation compatibility requires it, and add a build-command smoke bar before deleting the duplicate implementation.
- [ ] Retire proven dead compatibility surfaces in small ownership-scoped changes: [rendering/render_strategy.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/rendering/render_strategy.py) has no live callers and retains an obsolete busy-wait scheduler/update queue; [widgets/dimming_overlay.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/widgets/dimming_overlay.py) is referenced only by tests after GL compositor replacement; [sources/rss_source.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/sources/rss_source.py) is a facade with test/doc references but no production caller; and `transitions/overlay_manager.py::_raise_halo_topmost` is a called compatibility no-op. For each, prove no dynamic/frozen-build consumer before removing both implementation and tests that exist only to keep dead code alive.
- [ ] Collapse deprecated `DisplayWidget` class-global input authority into `MultiMonitorCoordinator`. `_global_ctrl_held` and `_halo_owner` are still actively read/written by `rendering/display_input.py`, `rendering/display_native_events.py`, and `input_handler.py`, leaving two mutable authorities despite the coordinator contract. Migrate one field at a time with multi-display Ctrl/edit/halo ownership tests, then remove declarations only after runtime and frozen-build call-graph checks.
- [ ] Split the slow [tests/test_widgets_tab.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/tests/test_widgets_tab.py) monolith by lazy settings-section ownership. Its 81 tests / roughly 2,500 lines took about 65 seconds for the main class and 80 seconds for the remaining non-Blob selection in isolated measurements, which made quiet runs resemble hangs. The bounded subprocess runner now preserves other shard results and enforces a timeout; follow up by separating family files/fixtures, minimizing full-tab construction where descriptor/section tests suffice, and retaining at least one integrated lazy-hydration/save bar per section.
- [ ] Add a lightweight AST test-topology bar for accidentally nested pytest cases. This audit found six Bubble gradient/settings functions nested inside another test plus one ThreadManager recurring-timer test nested inside a timer-context test; all seven were invisible to collection until repaired. Permit intentional local helper/fake methods such as decorator subjects and fake `test_connection` methods, but fail when a `test_*` function with pytest-style fixtures/markers is defined inside another collected test.
- [ ] Retire deprecated Imgur end to end rather than repairing it. Remove its dev gate, defaults/settings model and UI, descriptors/factory/runtime widget, direct-network fallback, overlay-raise TODO, CUSTOM payload support, tests, packaging references, and current documentation once migration coverage proves legacy Imgur settings are ignored/stripped safely. Until that removal is promoted, do not optimize its provider, threading, layout, styling, or CUSTOM behavior; absence/migration safety is the only worthwhile new coverage.
- [ ] Extend test portability policy beyond the paths repaired in this audit. Production-relevant absolute file reads in `tests/test_widgets_tab.py` and `tests/test_visualizer_settings_plumbing.py` were repository-relative candidates; remaining absolute-read matches are predominantly deprecated Blob tests, while explicit Windows path strings used as parser fixtures are valid. Add a narrow static bar for real `Path("F:\\...").read_*` / `open("F:\\...")` operations rather than banning synthetic path fixtures.
- [ ] Keep test process state isolated from live installations by preserving both `APPDATA` and `LOCALAPPDATA` redirection in `tests/conftest.py`. This audit found no tracked credential artifact; `resources/client_secrets.json` remains ignored and untracked. Add a regression bar that tests/default tools cannot resolve undo, cache, credential, or generated-state paths into the user's live profile, and never open or copy ignored credential files merely to prove ignore coverage.

## Correctness And Privacy Validation Debt

- [ ] Add a two-profile Steam isolation matrix around [core/steam/cache.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/core/steam/cache.py), [core/steam/credentials.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/core/steam/credentials.py), [core/steam/achievement_pulse_cache.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/core/steam/achievement_pulse_cache.py), and [core/steam/abandonment_cache.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/core/steam/abandonment_cache.py). No present cross-profile leak was found, but opaque profile-key routing is a privacy boundary: prove two identities with overlapping app data get distinct cache paths, reads, rotations, artwork, and settings labels, while logs contain neither credentials nor raw identifiers.
- [ ] Freeze the Steam credential lifecycle in normal and MC packaged builds across OpenID callback cancellation, invalid API key, disconnect, restart, failed validation, and secure-desktop URL handoff. Cover [ui/tabs/widgets_tab_steam.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/ui/tabs/widgets_tab_steam.py), [core/steam/credentials.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/core/steam/credentials.py), and [core/steam/openid.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/core/steam/openid.py); scan logs, exports, cache records, and generated artifacts for API keys/passwords before treating the bar as complete.
- [ ] Expand Steam cache adversity tests beyond ordinary corrupt JSON: malformed/truncated records, unknown schema versions, wrong-profile records, stale successes, failed/private envelopes, and successful empty payloads must remain non-authoritative where appropriate and must never freshen timestamps or cross identities. Keep quarantine loud and exercise both Achievement Pulse and Abandonment consumers.

## Safe Optimization Candidates

- [ ] Consolidate bounded Steam settings-hydration cache reads behind one shared worker result. [ui/tabs/widgets_tab_steam.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/ui/tabs/widgets_tab_steam.py) currently hydrates Achievement labels and Abandonment library choices through separate exact-path passes; preserve signal-silent UI population and provider/decryption bans, then instrument one settings open to prove bounded reads, unchanged labels, and no stale-label regression.
- [ ] Remove redundant Steam artwork scaling only after measuring the current decode/prepare/paint chain in [widgets/abandonment_issues_widget.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/widgets/abandonment_issues_widget.py), [widgets/steam_card_widget.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/widgets/steam_card_widget.py), and [widgets/steam_components.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/widgets/steam_components.py). Target one worker-prepared image per presentation/DPR/target-size key and no steady-state paint scaling; require pixel-sharp 100/125/150/200% DPR comparison renders before changing ownership.
- [ ] Correlate Steam presentation commits with compositor/UI pressure before optimizing callback flow. Pair [widgets/abandonment_issues_widget.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/widgets/abandonment_issues_widget.py) and [widgets/steam_card_widget.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/widgets/steam_card_widget.py) commit/repaint timing with [rendering/gl_compositor_pkg/compositor_metrics.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/rendering/gl_compositor_pkg/compositor_metrics.py) during activation, provider refresh, cache-only rotation, transition collision, and settings rebuild; optimize only a measured owner and do not add retries, timers, or broad refreshes.
- [ ] Document and test the compatibility boundary between [rendering/gl_compositor.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/rendering/gl_compositor.py) and [rendering/gl_compositor_pkg](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/rendering/gl_compositor_pkg). The package is active and not a deletion candidate; prevent new duplicate behavior in wrappers, then require import-graph, frozen-build, and delegation tests before consolidating any method.
- [ ] Add a repeatable repository-hygiene bar that checks ignore coverage for `.tmp`, screenshots, logs, caches, build outputs, and credential files; rejects tracked runtime markers and generated preview images; scans tracked content plus the staged diff for API-key-shaped values/private Steam payloads; and verifies generated defaults artifacts match the canonical source. Keep this bounded and explicit so ordinary local diagnostics can remain on disk without becoming Git debris. The 2026-07-14 audit found ignored/untracked `resources/client_secrets.json` as intended and no tracked real credential file; preserve that boundary without reading local secrets into logs.

## Backlog

- [ ] Unify the fragmented painted-shadow direction authority before exposing an eight-direction General setting. The canonical `widgets.shadows.offset` value currently round-trips through settings/model/defaults but is not consumed by ordinary runtime paint paths: `BaseOverlayWidget`, Clock, Spotify volume, and visualizer cards read `PAINTED_FRAME_SHADOW_TUNING`; text/header/icon/control helpers read separate import-time tuning dictionaries; Weather scales local icon/control offsets; Media has signed-safe local control-pixmap math; and analogue face/hands/numerals/timezone details retain local positive offsets. First introduce one cached signed-vector resolver that preserves each surface's authored magnitude, then replace right/bottom-only `card_shrink_right` / `card_shrink_bottom` assumptions with four-sided visual padding and include the resolved vector in every affected DPR/cache key. Prove all eight directions plus bottom-right default across framed cards, text, headers, artwork/icons, controls, Weather, Media, Steam cards, Spotify volume/visualizer, and analogue Clock at multiple DPRs; require no clipping, content/outer-rect movement, per-frame setting lookup, new `QGraphicsEffect`, repaint loop, or UI-thread work. Only after that bar should General gain the inset 3x3 selector and persist a normalized direction/vector; decide explicitly whether `shadowtuning.json` remains magnitude authority or is migrated so two settings sources cannot conflict.
- [ ] Classify the 13 active-looking Bubble widget tests that currently exit through `_apply_authored_bubble_debug_preset()` because retired Preset 9 no longer exists. Do not restore that debug preset or retune working Bubble runtime merely to make old fixtures run. For each caller, either prove the unchanged oracle remains meaningful against the current Deep Sea authored baseline and retarget only its setup, or convert/remove it as explicit U-07 retirement evidence so collection no longer implies active coverage that silently skips. The 2026-07-15 live Drift evidence remains independently healthy and must not be weakened to accommodate stale Preset 9 goalposts.
- [ ] Close the remaining runtime half of the prior CUSTOM edit-shell oracle concern. The complete offscreen manager suite passed `94/94` on 2026-07-15, including reset-button containment and peer-guide ordering, so the earlier four failures are no longer reproducible in the current environment and must not remain described as active automated failures. Run one real multi-widget edit session with bundled runtime fonts to confirm narrow shells contain both reset controls and semantic peer guides remain visible/ordered; if that passes, retire this item rather than preserving a stale oracle narrative.
- [ ] Audit a dedicated shared visualizer animation-clock migration from wall time to `time.monotonic()` with explicit clock-jump injection plus Bubble/Spectrum/Sine/Oscilloscope locks. An earlier unscoped clock swap broke five dt-dependent reactivity tests; keep any future clock migration isolated from deprecated Blob removal and require supported-mode runtime-shaped evidence before changing the shared source.
- [ ] Reconcile the unrelated curated Spectrum preset mirror drift detected during prior validation: `presets/visualizer_modes/spectrum/preset_1_organs.json` now has `spectrum_lane_strengths_raw.Bass = 0.1794871794871802`, while `release/main_mc.dist` still carries `0.24`. Confirm which authored value is intentional, then regenerate/sync the release tree without involving deprecated Blob code or tests.
- [ ] Continue correlating the 2026-07-12 transition/visualizer DT and paint-gap spikes (roughly 100-180 ms) as a shared-pressure investigation. Fresh 2026-07-14 Spectrum windows narrowed the visible anomaly: smoothing remained active at about 99 FPS, while input/support coherently collapsed to zero around 09:46 and isolated 47-76 ms tick gaps appeared around 10:41. A solid-Spectrum-only zero-frame hold and catch-up bound now cover that display symptom; do not use it as evidence for a shared cadence/clock rewrite or add UI refresh pressure.
- [ ] Review image-pipeline `scaled-miss` worker-fallback telemetry. Worker preparation is the correct recovery route, but repeated normal cache misses should not look like exceptional fallback noise; change level/copy only after confirming no decode/prescale churn or source-size instability.
- [ ] Review repeated Spotify visualizer reveal-watchdog warnings emitted after each settings restart while `playing=False`, `require_playing=True`, and `authoritative_media=False`. Confirm playback still reveals promptly when authority returns, then treat the expected idle/no-media wait as ordinary state rather than warning-level failure noise.
- [ ] Split Spotify visualizer latency health by playback authority. The 2026-07-14 merged perf report counted 12 severe latency probes, but all were explicit transition probes against paused/stale audio rather than live playback. Preserve the probes and raw latency telemetry, add the new overlay `playing=` state to the classifier, and report paused forced-probe diagnostics separately so they cannot hide or falsely imply a live-audio latency regression.
- [ ] Reclassify successful Reddit cache telemetry currently emitted at `WARNING` if fresh logs confirm it is ordinary success. Preserve warnings for stale/failure/fallback authority changes rather than muting the underlying provider/cadence guards.
- [ ] Reconcile likely-stale Media/Visualizers descriptor tests in [tests/test_widget_descriptors.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/tests/test_widget_descriptors.py). Three expectations still assume older Media/Visualizers coupling: `media.loader_guard_attrs == ("media_enabled", "vis_enabled_checkbox")`, mutual lazy dependencies between `media` and `visualizers`, and programmatic Visualizers hydration materializing Media first. Current [rendering/widget_descriptors.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/rendering/widget_descriptors.py) has Media and Visualizers split more cleanly (`media` only guards on `media_enabled`; Visualizers depends programmatically on Defaults only). High likelihood: stale tests after intentional settings-section split. Before deleting/changing assertions, verify Media/Visualizers settings persistence, bucket state, scroll state, and runtime visualizer enablement still work when opening either section lazily.
- [ ] Revisit [rendering/gl_compositor_pkg/__init__.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/rendering/gl_compositor_pkg/__init__.py) and decide whether it should remain a clearly quarantined compatibility shell or be folded into a cleaner package-facing contract.
- [ ] Retire or quarantine [rendering/render_strategy.py](F:/Programming/Apps/ShittyRandomPhotoScreenSaver/rendering/render_strategy.py) as part of the dead-surface audit item above. Current `rg` shows no live imports/callers, but the file still contains an old busy-wait timer loop and a separate update-queue helper; remove it only with a caller/frozen-build bar so dynamic import assumptions do not regress.
- [ ] Classify and migrate remaining direct `QTimer.singleShot` sites only when they stop being UI-local. Current lower-priority examples include message-box auto-close, settings scroll/notices/about-image refresh, settings save debounce, system-tray tooltip refresh, Gmail auth/status delay, and tool/test-only helpers.
- [ ] Investigate `ImagePrefetcher._submit_load.<locals>._on_done` callback `IndexError: pop index out of range` if it recurs. Latest rotated perf run showed it once in `core.threading.manager` after an image load callback; fix the source callback directly rather than adding retries or broad exception masking.
