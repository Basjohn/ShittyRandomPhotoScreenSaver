"""Per-visualizer UI builder modules extracted from widgets_tab_media.py.

Each module provides a build_*_ui(tab, layout) function that creates
widget controls for a specific visualizer mode and attaches them to the tab.
"""
