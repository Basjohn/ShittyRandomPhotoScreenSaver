# Geo Audit

Last updated: 2026-06-28

## Purpose

`audits/GeoAudit/Visualizer_Runtime_Shape_Audit.md` is the historical/watchlist geometry audit for the visualizer CUSTOM/runtime shape family.

Reopen that file as the execution document only when fresh `--geo` evidence shows:

- route repair or participation fallback during ordinary CUSTOM saves
- duplicate visualizer ownership
- replay-green/runtime-wrong geometry
- unauthorized visualizer aspect/width drift
- top-left, square-creep, missing-visualizer, or recovery-primary behavior

This README stays intentionally small so the audit remains a reopen map instead of turning into another changelog.
