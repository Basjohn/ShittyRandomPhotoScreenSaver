# Acceptance Evidence — 2026-08-19 19:04–19:08 — Second Installed Run — FAILED

## Provenance

**PRIMARY RAW + CONTEMPORANEOUS REVIEW + PRIMARY OPERATOR**

Raw pack:

```text
a497d069-2ff3-4a03-b553-0c160f584e74.zip
SHA-256: 134f249a92f17b8885f9bf040834dba92156d5ef9b84132b501cf35f4fea6a18
```

The raw log predates `[SOURCE_HEAD]`.

Contemporaneous findings identify tested source anchor:

```text
ccb63542348fec5993a688142bc2e364f8149f6a
```

## Operator-visible result

- Spectrum idle fixed.
- Pause/Play hitch unchanged.
- Fade-in start remains clean.
- General hitching remains.
- Transition performance remains poor.

Acceptance: **FAILED**.

## Generation zero — FIXED

```text
Runtime started (generation=0 interval_ms=11.11)
```

## Spectrum idle — FIXED

Real renderer:

```text
count=35
min=0.0738
max=0.4192
```

Operator confirms visible resting bars.

## Logical runtime

```text
generation=0 steps=12488 skipped_deadlines=11 slow_steps=2 failures=0
generation=0 steps=1699  skipped_deadlines=3  slow_steps=0 failures=0
generation=1 steps=5065  skipped_deadlines=5  slow_steps=2 failures=0
```

The repaired ~90 Hz class remains healthy on average.

## Pause/Play visible handoff collapse

Before rapid toggles:

```text
~81.6 state/update handoffs per second
```

During:

```text
~68.7/s
then
~56.0/s
```

After:

```text
~83.1/s
```

Meanwhile logical runtime remains ~89.9 Hz.

Conclusion:

```text
simulation can stay healthy
while
GUI consumption/presentation collapses
```

## Feedback / media paint

Feedback paint requests/event include:

```text
23 .. 75
```

Rapid-toggle `media.paint` windows:

```text
19:07:50 calls=50 avg_ms=3.87
19:07:51 calls=50 avg_ms=2.62
19:07:52 calls=50 avg_ms=1.87
19:07:54 calls=50 avg_ms=2.85
19:07:56 calls=50 avg_ms=3.45
19:07:59 calls=50 avg_ms=2.46
```

Conclusion: dirty-region update alone did not eliminate real parent paint execution.

## Physical presentation

165 Hz, seven completed Blockspin windows:

```text
140.7
144.8
140.0
138.1
136.3
136.5
141.3 FPS
```

Raw parser median:

```text
140.0 FPS
```

Request acceptance:

```text
87.78% .. 92.38%
median 89.42%
```

60 Hz:

```text
55.5 .. 58.2 FPS
median 56.8
```

## Delivery-stage attribution

High-refresh windows repeatedly show:

```text
wake lateness p95 ~1.2–2.4 ms
paint_pending_skips=0
dispatch_pending_skips dominant
dispatch-skip age p95 up to ~95 ms
```

Conclusion:

The current timer generally wakes; GUI dispatch is not servicing the requested presentation promptly.

## Hypothesis generated by this run

The source audit identified a synchronous GSMTC wait as a strong Pause/Play-specific defect.

Important qualification already known here:

```text
core/media/media_controller.py was byte-identical to accepted baseline 42033c84
```

Therefore it was a legitimate defect to correct, but not proven to be the whole regression owner.
