# Steam Games You Follow | Live feasibility-to-release decomposition

**Status:** G0 live linked-followed set (142 games) and bounded public-news sample (two apps, four usable news items) confirmed by operator; the opt-in G1 source/cache has an operator-reported 55-case Windows gate; an unadmitted G2 Qt model/QML and grouped geometry staging slice awaits its own native Qt test. The live widget/runtime is not yet admitted. Product label **Games You Follow**, existing compatibility identity `steam_progress`, existing `--devsteam` gate for the unfinished replacement only. This replaces the unfinished Steam Progress/Journey scaffold's product meaning; it is not an owned/recent/wishlist feed, personalised Steam-wide ranking or Store scraper. Tick each phase only with its evidence; no checkpoint diary belongs here.

## G0. Source/security admission | mandatory before product service or GUI

- [ ] Trace the current credential/private Steam source path (`core/steam/backend.py`, request policy, profile state, cache, assets, action router) and current `steam_progress` settings/caller/descriptor scaffold; capture actual request wrapper and version before adding a source. Pin pre-feature revision/GODZIP for rollback without copying old scaffold visual code into the new card.
- [x] **Probe preparation:** The existing backend now has a `GAMES_FOLLOWED` source metadata entry plus a read-only, explicit-CLI probe. Observed Steam API metadata lists **SteamID64 only**, without a required Web API key for `IStoreService/GetGamesFollowed/v1`; this is *not* proof of live access for this account or of the response schema. The probe reads the already-linked SteamID64 from existing encrypted credentials but sends no key, emits only status/count, performs one bounded request and makes no cache/Settings/runtime changes. Fake fixtures reject malformed/unbounded app lists and distinguish genuine empty from denial/errors. No source or widget has been admitted.
- [x] **Live identity/access proof:** Operator ran the one-shot existing-identity G0 CLI on Windows and reports `confirmed_nonempty`, count 142; Steam-focused gate 14 passed. No raw account identity, followed-list payload or credentials should enter diagnostics, UI, Settings or artifact handoff. This proves followed-set access only, not APP_NEWS admission, privacy/export audits or a finished widget.
- [x] Fixture-prove *probe-only* bounded `ISteamNews/GetNewsForApp/v2`: the explicit `--news-probe` verifies the real followed set, then checks only its first two AppIDs, each with `count=2`, 192 KB and 6 s request limits. Exact response AppID, bounded unique GID/title/date, confirmed empty vs 401/403/429/malformed/oversized are covered by deterministic fixtures. No arbitrary whole-library scan, article/image fetch, source timer, app-key request or QML admission. AppIDs are redacted from backend logging; status/count only to stdout. The operator subsequently confirmed four usable news items from two followed apps in the read-only Windows news probe. The 2-app G0 sample is a safety limit, NOT the eventual G1 request budget or accepted candidate count.
- [x] Run the explicit G0 live news CLI against the already linked identity. Operator reports `confirmed_nonempty_news`, `apps_checked=2`, `usable_items=4`; this does not validate links, artwork or a full followed-set news scan.
- [ ] Review the provisional G1 request budget and visible story cap against Windows runtime results before admitting periodic source work: at most four app-news queries per explicit refresh, three news entries per app, eight accepted stories, with a caller-supplied bounded window offset across follows. Keep the existing Steam refresh coordinator as the sole eventual scheduler. A two-app G0 sample is not a promise that all 142 follows have news.
- [ ] Independently validate source-article **HTTPS and redirect** policy with the existing secure-helper/action route, and news-image reference host/scheme/byte/signature policy with the existing asset cache. A pure `news_article_target` policy now rejects anything except the exact canonical `https://store.steampowered.com/news/app/<appid>/view/<gid>` of the accepted app and news item (wrong app, host, port, userinfo, query, fragment and malformed URL fail closed). This is **not** permission to launch provider URLs: helper/browser redirect handling, noncanonical legitimate Steam article variants and runtime revalidation remain unresolved. Until proved, headline action disabled, app-art fallback only; no raw arbitrary URL in QML.
- [ ] Confirm credentials/SteamID remain in the existing private source/account store, never Settings, QML roles, logs, cache filenames, exported SSTs or test fixtures. Use only redacted deterministic fixtures in automated suite. Record G0 success/failure and exact evidence; neither a speculative source nor a GUI mock may be marked released.

## G1. Neutral source/cache and bounded runtime | only after G0

- [x] **Validate G1 source/cache candidate on Windows:** `core/steam/games_followed_source.py` is a caller-invoked neutral source, not a widget or refresh scheduler. It stores only bounded normalized private story identities/headlines/timestamps in an existing opaque-profile cache, never raw response, HTML, provider URL or credentials; it returns cached last-good indefinitely after source failure, rejects a linked-profile identity mismatch before cache/network access, and one request generation fences late retirement. Its G0 input is the already verified linked follow set. The operator reports the focused 55-case Windows test gate passed. Secure action validation, actual live shared runtime admission, and account-private cache/export review remain separate unchecked gates below.

- [ ] Define one source/provenance contract: authoritative followed set, accepted news per admitted app, source timestamp/quality, `available`, `empty_follow_list`, `private`, `unavailable`, `no_usable_news`, `stale_cache`. No false empty state on source failure. Source ownership is explicit follows only, never candidate activity or Steam ownership.
- [x] Reuse `SteamRequestCoordinator`, **existing `SteamBackoffPolicy`**, backend status/redaction, locks and atomic `SteamCacheRecord` envelope and opaque account-private profile cache in the caller-invoked G1 source. Denial/rate limit/network failure arms bounded cooldown on the existing source key; there is no retry timer, second refresh scheduler or repeated endpoint call during cooldown. The runtime shared owner and its actual cadence/leases still need admission. Keep **last-good followed set and last-good admitted news indefinitely** after freshness window on failure, marked cached/stale; failures/invalid responses never freshen or delete it. Reset/identity change/schema corruption are explicit deletion boundaries. No separate per-display cache or request scheduler.
- [ ] Normalize minimal bounded story fields: AppID and stable news identity private to runtime; title/headline/published/feed/tags; one policy-approved article target and image reference, if any. Deterministic dedupe/ranking (one or more items per app only if product choice explicit; newest timestamps then stable IDs), hard cap before enrichment. Avoid a long-lived read-history/dismissal DB and avoid raw payload caching.
- [ ] News item image first only if G0-proven, else existing `core/steam/assets.py` shape-matched art (`wide` default, `square`, `portrait`), at most one proven alternate shape on definitive missing/invalid. Local-file-only artwork role, bounded byte/image validation, retained `ArtworkFadeImage` behavior, selected visible row-only hydration and revision/app/item/shape/source/generation fencing. Never fetch article pages to hunt media.
- [ ] One runtime-generation shared owner plus per-display leases under existing Steam family/member/developer/real-consumer gates. Existing canonical `widgets.steam.refresh_minutes` owns periodic cadence. Manual refresh uses same coordinator. Unchanged accepted snapshot is a no-op for model rows/artwork/rendering; Settings opening starts no backend. Last lease stops feature-only refresh/assets, fences late work and preserves other Steam cards' legitimate shared owners.
- [x] Staged retained Qt list model and coherent accepted revision: `games_followed_projection.py` returns bounded plain-text headline/feed/date/quality and opaque ordinal slots only; `FollowedStoryRows` exposes exactly eight stable display-only slots with narrow `dataChanged`, no `modelReset`, source identifiers or URLs. Local packaged Steam logo only. Article actions and remote images are **disabled**; actual private selected-row action bridge, shared runtime/lease and Windows Qt test are still separate unchecked admission gates.

## G2. Current retained Qt Quick widget and CUSTOM geometry | first GUI slice

- [ ] Admit the staged Qt family through the **existing** ordinary descriptor/registry/host using the retained `GamesYouFollowPresentation.qml`; no extra QWidget/QQuickWidget/window/provider timer or custom geometry owner. Stable identity remains `steam_progress` until a persisted-schema/slot migration is explicitly proven and admitted. The primary layout is **fully adaptive** across independently edited width and height, not a fixed single/double-row product variant. Keep a bounded source-selected story set and a truthful overflow summary when available space cannot show every admitted story. Never silently truncate source data or change the actual followed games because the card is compact.
- [ ] Reuse the family Steam header, shared theme roles and shared `ArtworkFadeImage`. Present game/title context, headline, honest source/time labels, safe news-image-first/app-art fallback; shape controls `wide`/`square`/`portrait`. Typography settings include font family/size, alignment `left`/`center`/`right` and bounded `headline_truncation_chars` followed by measured QML line clamp/elision; optional event-owned tooltip only when truly elided, no marquee/carousel/polling.
- [x] Stage **four stable semantic child roles** in the presentation-neutral `rendering/games_followed_child_roles.py`: `header`, hidden inactive `refresh`, **one grouped `story_tiles` block**, and `overflow_summary`. QML role declarations remain independent of provider, content, live extent and visibility; the existing shared CUSTOM owner will persist these, never per-story positions. Applied-transform/paint-proxy identity, Edit lifecycle and Save/reopen across real admission remain unchecked native gates.
- [ ] Repeated stories remain **one family-authored, responsive layout with stable semantic story IDs**. The pure `widgets/steam_followed_layout.py` contract now bounds capacity across independent X/Y and retains wide/tall arrangement inside a resize hysteresis deadband, without fetching or mutating the source. Its unadmitted Qt model/QML implementation and retained delegate tests now exist, while shared host admission and Windows execution remain open: an extreme-wide card shows several readable horizontal news entries (side-by-side rail), an extreme-tall card shows several vertically arranged stories, and an approximately square card shows several grid entries. Use independently available width/height, measured minimum card/typographic/artwork bounds, deterministic column/row admission and a small aspect-ratio switching deadband to prevent flip-flop during live drag. Selected story identities/order do not change across shape transitions; hide only complete entries when space is insufficient, and reveal them again on growth without refetching or rebuilding the role list. Keep a configurable bounded maximum accepted/visible story count separate from visible capacity; never use a QML Repeater model whose identity churns every pointer event. If the operator requests reorderable columns, **reuse the accepted Reddit/Gmail widget-wide semantic column-rail contract**; do not duplicate per-row handles, reorder provider data or add an ordering authority.
- [ ] Declare truthful preferred outer size and **both** independent shared `content_extent` axes from first implementation. Side X/Y changes reflow available story-card artwork/text and visible capacity (wide rail / tall column / near-square grid), but do not refetch, re-rank or replace the canonical bounded story set. Selected-parent diagonal changes both, square corners/wheel uniformly scale the complete reflowed card. Shared session owns Save/Cancel/Undo/slots/Restore/position/monitor and floor admission. Edited child X and Y are separate live authored-rail deltas; ordinary layout/flip remains family-owned. Child moves cannot grow parent or publish child-required extent, collisions OFF still respects real containing surface. No family-local Settings geometry writes, QML resize timer, parent-size feedback or extra normalization authority.
- [ ] Header may flip **placement only**, never bitmap/text pixels; test source/game/title/AGO/source lanes at compact independent X/Y after prior child edits, reset, save/reopen, multi-display generation. Layout density/story cap and other Settings preferences are configuration-owned, CUSTOM geometry is session-owned; slots restore layout-relevant configuration and ordinary ON/OFF/CUSTOM geometry without smuggling source/account/cache state.
- [ ] Store artwork and headline emit narrow semantic action plus stable selected row handle; Python revalidates private current AppID/approved URL on click and routes via existing Store/secure-helper policy. A helper rejection must not open a raw URL or terminate the screensaver. No QML raw URL navigation.

## G3. Canonical Settings and lifecycle

- [ ] Keep exactly one `widgets.steam_progress` canonical defaults/settings/persistence root for v1, with Games You Follow labels; retain caller-proven existing position/font/card-style fields, introduce only enabled, bounded story-cap/density, alignment, artwork shape, truncation and justified visible chrome choices. The automatic horizontal/vertical/grid reflow is an intrinsic family behaviour, not an unrelated persisted mode or layout owner. The staging model uses a provisional base extent; the admitted adapter must derive authored dimensions from this canonical root rather than creating a second defaults authority. Existing Steam credentials, privacy and refresh remain in their current owners. No second `widgets.games_you_follow` schema, shadow defaults JSON or state copied into CUSTOM.
- [ ] Update settings buckets/registry/predictor/preview and canonical defaults/constraints/migration/SST/slot tests in the same slice; lazy Settings load never imports/starts a provider. Do not flatten authored presets or use the unfinished Steam Progress mock visuals as a parity target.
- [ ] True dormancy when any gate fails: Steam family activation, member ordinary enabled, `--devsteam`, authorized source configuration and real presentation consumer. Last-display retirement, app disable, account change and generation replacement fence requests/image callbacks and release retained model/item without shutting down Steam siblings.

## G4. Acceptance matrix and exit

- [ ] **Source/privacy tests:** key-path fixtures and live-gate evidence, finite follow/news budget, malformed/429/private/stale/empty statuses, secure-helper redirect policy, news-image byte/signature rejection, no leaked ID/key/URL into Settings/QML/logs/export, wrong-source substitution forbidden.
- [ ] **Owner/cache tests:** one coordinator/cache/runtime across displays, last-good indefinitely readable, no freshness reset on failed response, stable list ID/no-op, selected-row-only image work, stale completion drop and zero work with no consumer.
- [ ] **Qt geometry/lifetime tests:** wide horizontal rail with several stories, tall vertical column with several stories, square multi-story grid, aspect-boundary hysteresis, compact hide/reveal and expanded independent X/Y, artwork shapes/crop states as configured, text elision and overflow, semantic flip if offered, proxy exact painted identity/bounds/clipping and 20+ normalization/content updates with same delegate; hide/reveal, Save/Cancel/Undo/Restore/slots/reopen, off-Edit zero observers, no QML transform-list warnings or live-parent feedback. Authored siblings stay in normal row rails when one singleton role is edited.
- [ ] **Performance tests:** baseline/candidate event-loop latency and retained objects, no repeated network/image/model/layout work per pointer event, no re-schedule on identical source data, correct Steam-sibling cost and retirement; don't infer performance from FPS only.
- [ ] **Physical smoke only after tests:** one cached and one fresh authorized snapshot, independent X/Y resizing through wide rail/tall column/square grid, correct artwork/news/article action and one disable/re-enable/Save/reopen; record exactly what automated tests could not prove. A source that failed G0 has no physical product test.
- [ ] Mark implementation accepted in `Current_Plan.md` only with explicit Windows source, Qt/security, lifecycle and performance evidence; remove completed tasks from live queue rather than append checkpoint history.
